# Gowith UI/UX Motion Design（已废弃）

> 本文件不再作为动效规范。视觉和动效规则已合并到唯一生效文档：[Gowith-Design-System.md](./Gowith-Design-System.md)。
>
> 以下内容保留为历史记录，不应继续用于新页面或动效实现。

版本：1.0  
适用平台：iOS 17+ / SwiftUI  
状态：动效与视觉灵感规范  
替代规范：[Gowith-Design-System.md](./Gowith-Design-System.md)

## 1. 文档目的

本文件将五张 UI 参考图中的视觉语言、组件结构和动效启发转化为 Gowith 可执行的设计原则。它是后续 UI/UX 动效设计的灵感来源与约束，不是对参考画面的逐像素复刻。

当本文件与产品流程、可访问性要求或 `UI-system.md` 冲突时，优先级如下：

1. 产品任务正确、状态清楚、操作可靠。
2. iOS 系统行为与可访问性。
3. `UI-system.md` 中已确认的布局和视觉决策。
4. 本文件中的参考图灵感与动效建议。

## 2. 参考图观察摘要

### 2.1 数据仪表盘：进度图形与高对比

参考特征：浅色页面搭配深色数据区域，荧光绿突出关键数值，圆环进度显示目标完成程度，页面按“总览数据、当前目标、后续内容”组织。

可转化为 Gowith 的灵感：

- 清点进度可用环形或线性进度表达，但旁边必须同时显示清楚的文本和数量。
- 青绿色只强调当前进行中、已确认或抵达等少数重要状态。
- 进度动画用于解释状态改变，不作为常驻装饰。

避免：大面积黑色主题卡片、荧光色大面积铺底，以及让图表代替具体物品状态。

### 2.2 订阅管理：数字摘要与可扫描清单

参考特征：大数字提供总览，彩色区域区分套餐，列表行组合图标、名称、日期和金额，新增操作固定在易发现的位置。

可转化为 Gowith 的灵感：

- 背包或地点的物品数可以作为简短摘要。
- 物品行采用“图标/缩略图、名称、状态、单一快捷操作”的稳定顺序。
- 统计必须带有语义标签，不能只呈现孤立数字。

避免：促销式大色块、同屏多个高权重数字，以及让浮动新增按钮遮挡清单。

### 2.3 挑战与健身计划：3D 主视觉和空间层次

参考特征：色块卡片、挑战横幅、日期选择、不同尺寸的计划模块，以及醒目的 3D 主题物件和人物模型。

可转化为 Gowith 的灵感：

- 房屋、背包、相机等实体对象可逐步采用统一风格的 3D 图标。
- 3D 视觉优先服务对象辨认、地点识别和关键空状态。
- 选中背包或地点时，可用轻微位移、缩放或阴影变化表达焦点。

避免：在连续清点列表中让大型 3D 物件挤压名称、状态和点击区域；避免多个不同材质、比例和光照风格混用。

### 2.4 行程任务：当前任务、关键指标与待办列表

参考特征：标题和解释文案建立页面上下文，主任务区域突出今天计划，少量指标补充状态，随后是可直接处理的任务列表。

可转化为 Gowith 的灵感：

- 页面先告诉用户当前地点、背包或出行状态，再提供下一步动作。
- 活动出行页可按“当前行程摘要、关键状态、物品清单”组织。
- 任务完成后在原上下文内更新状态，不把用户跳转到无关页面。
- 线路或细线图形可以表现地点之间的流转，但不得混淆真实地图信息。

避免：重复堆叠黑色大卡片和多层嵌套面板。

### 2.5 智能家居：清楚的控件状态和地点语境

参考特征：顶部呈现家庭环境与房间选择；音乐、温度和设备开关各有对应控件；图标、文字和控件状态相互配合。

可转化为 Gowith 的灵感：

- 家或地点切换需要让当前地点始终可辨，并通过标题、图标和选中状态共同表达。
- 状态变化应在对应物品行或地点附近反馈。
- 多设备式控制面板的“每种操作使用合适控件”原则可用于背包数量、位置提醒和偏好设置。

避免：把物品清点改造成密集的两列控制面板，或仅靠颜色表达开关/携带状态。

## 3. Gowith 的动效目标

动效需要承担至少一种明确职责：

- **引导注意力**：指出当前可执行的主要操作。
- **解释状态变化**：说明物品已装入背包、已带回或进入待确认。
- **维持空间连续性**：让用户理解背包、地点和行程之间的关系。
- **确认操作结果**：提供及时、克制的成功或失败反馈。

动效不应只是“让界面看起来更忙”。如果静态状态已经足够清晰，优先保持静态。

## 4. 动效原则

### 4.1 短、轻、可中断

- 常规状态反馈目标时长：150–220 ms。
- 页面层级切换目标时长：约 180–280 ms；系统导航和 sheet 优先使用原生转场。
- 较长的对象连续运动只用于重要流程说明，通常不超过 350 ms。
- 动画期间仍应保持控件可理解；允许用户立即继续操作，不等待动画结束。

### 4.2 由交互触发，不自行抢焦点

- 优先响应点击、滑动、确认、到达事件等明确触发。
- 不自动轮播，不让装饰动效循环播放。
- 到家提醒使用通知或明确的页面状态；不能只依赖地图上的动画提示。

### 4.3 反馈保持在操作附近

- 加入/移除物品时，优先更新对应物品行的图标、文字和状态。
- 选择背包时，焦点反馈发生在当前背包区域。
- 到达地点时，提示与目标地点信息关联。
- 不通过全屏闪烁、强震动或长距离飞入动画表达普通操作。

### 4.4 动画之外仍需有完整状态表达

- 已携带、已带回、待确认、遗失和已放入地点均需有文字或 VoiceOver 标签。
- 动画结束后，用户仍能从静态界面读懂当前状态。
- 不把位置授权、通知授权或后台定位是否工作隐藏在动画中。

## 5. Gowith 动效语法

### 5.1 过渡

- 页面内状态切换：轻微淡入淡出或短距离位移，避免整页大幅飞入。
- 系统页面导航、sheet 和确认对话框：优先采用 SwiftUI/iOS 原生呈现方式。
- 内容刷新：保持列表位置与页面结构稳定，只更新发生变化的行或数字。
- 分类切换：选中标识平滑移动，列表内容轻微淡出/淡入；不要令列表整体上下跳动。

### 5.2 物品加入背包

期望表达：物品从“地点库存/物品库”成为“当前背包内容”。

- 先即时改变原行的状态，再以短促的缩放、勾选绘制或轻量位移作为确认。
- 如果同时更新背包数量，只对数量做短暂的数字变化反馈，不让整个页面重排。
- 不要求物件真的飞到屏幕另一处；若采用共享元素转场，必须保证列表滚动、快速连续添加和 VoiceOver 下仍稳定。
- 动画失败或被系统关闭时，加入结果仍需立即可见。

### 5.3 物品移除或放回地点

- 更新物品所在位置和状态文字，保留行的位置稳定，避免突然消失造成误解。
- 只有用户执行删除时才考虑移除行；“放回地点”是状态变化，不等同删除。
- 成功后可使用轻量触觉反馈，且不以触觉作为唯一确认。

### 5.4 清点进度

- 数字、进度条或圆环以短时过渡显示进度变化。
- 更新必须跟随真实清点状态，不预先播放虚假的完成过程。
- 完成时可以出现一次轻量强调，例如勾选描边或简短淡入；不使用烟花、持续粒子或阻塞式庆祝动画。
- 存在待确认物品时，待确认数量和截止日期应比装饰动画更醒目。

### 5.5 地点与到家提醒

- 当前地点、目的地点和提醒范围必须先用文字/地图标记表达。
- 进入约 50 米地理围栏时，应用可触发系统通知或在前台展示到达提示；动画只作为补充。
- 到达提示可以用地点标记的轻微扩散/描边表达“进入范围”，不得暗示 GPS 精度达到室内或门口级别。
- 到达后由用户确认是否把物品放入该地点；系统不能仅凭进入围栏自动更改库存。
- 授权关闭、定位不可用或应用未获得后台触发条件时，仍提供手动确认入口。

### 5.6 3D 图标与空间表达

- 3D 图标应保持统一的透视角度、光照方向、材质、阴影和画布比例。
- 3D 图标用于对象识别和少数聚焦场景；常规列表仍使用固定尺寸缩略图或 SF Symbols。
- 首期优先提供静态 3D 图标；动画只在选择、装入、到达等有语义的状态中轻微改变姿态或位置。
- 不将 3D 图标误称为真正实时 3D 场景，也不要求每个用户照片自动转换为 3D。
- 动画不得改变列表行高、造成内容重排或削弱文字对比度。

## 6. 视觉与布局指导

- 页面保持 `UI-system.md` 确认的连续纵向布局、轻量分隔和适量留白。
- `go with` 字标可出现在页面左上区域，保持品牌识别但低于当前页面标题和主要任务。
- 以黑、白和中性灰构建基础层次；亮青绿色作为稀缺强调色，按固定语义使用。
- 辅助色可以用于主题图标、类别和地点标记，但避免多个大面积高饱和块同时争夺焦点。
- 3D 图标与细线、路线、建筑轮廓等图形可以组合；组合图形必须让真实信息优先可读。
- 优先使用系统字体和系统图标；自定义图形应有明确含义，并具备无障碍描述。
- 不恢复已移除的黑色顶部主题卡片、毛玻璃底部 Dock 或悬浮 GO 按钮。

## 7. SwiftUI 实施建议

实现时优先使用 SwiftUI 原生、可访问且可中断的动画 API：

- `withAnimation` 配合明确的 `Animation` 曲线，用于局部状态变化。
- `.contentTransition(.numericText())` 用于计数变化，避免手工滚动数字动画。
- `matchedGeometryEffect` 只用于存在明确空间连续性的少数转场，并提供不使用该效果时同样清楚的状态反馈。
- `sensoryFeedback` 用于少数关键确认，避免每次列表滚动或普通选择都触发触觉。
- `accessibilityReduceMotion` 开启时，替换位移、缩放和视差效果为淡入淡出或即时状态更新。
- 使用原生 `NavigationStack`、`TabView`、`sheet` 和系统通知行为，不重造系统导航转场。

示意代码：

```swift
withAnimation(.easeOut(duration: 0.18)) {
    isPacked.toggle()
}
```

以上仅为局部状态变化示意。实际代码需遵循项目部署版本、状态模型和系统可访问性行为。

## 8. 可访问性、性能与降级

- 尊重系统“减少动态效果”；关闭后不使用大幅位移、缩放、视差或持续动画。
- 支持 VoiceOver：状态变化需通过可访问性值或适当的 announcement 传达，避免频繁打断用户朗读。
- 支持 Dynamic Type；布局不能因动画固定高度而裁切文字。
- 避免高频模糊、粒子、复杂阴影和屏幕级持续重绘，特别是地图与长列表同时显示时。
- 定位、通知和网络操作的加载状态需有文字提示及失败恢复路径。
- 动画被系统减少、应用进入后台、视图快速切换或用户连续操作时，数据状态仍必须正确。

## 9. 验收清单

- 动效能够说明状态改变或引导下一步，而非纯装饰。
- 动画结束后，状态仍能通过文字、图标或控件清楚识别。
- 连续添加多个物品时，列表不跳动、不丢操作，也不改变滚动位置。
- 待确认项目、到家提醒和围栏范围不依赖颜色或动画单独传达。
- 位置进入提醒只表示进入设定区域，不暗示精确室内定位。
- 3D 图标尺寸稳定，不挤压名称、状态和触控区域。
- 开启“减少动态效果”后，关键流程仍完整可用。
- 动效在 iPhone 竖屏、动态字体放大、VoiceOver 和低性能状态下均不阻塞操作。
- 页面遵循 `UI-system.md`：连续布局、系统导航、不过度使用卡片、不恢复悬浮 Dock/GO。

## 10. 后续扩展边界

本文件目前定义视觉与动效方向，不代表下列能力已进入当前开发范围：

- 动态 3D 人物或长期陪伴型角色。
- 地铁站、特殊景点的全场景动画。
- 用户照片的云端 AI 抠图或 2.5D 渲染。
- 复杂路线轨迹、实时共享或社交挑战。
- 主题商店、付费职业主题及摄影师设备素材包。

这些方向可以在未来用于主题和品牌体验，但应在核心的地点库存、背包流转、定位提醒和清点体验稳定之后单独评估。
