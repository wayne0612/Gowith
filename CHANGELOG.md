# 改动日志

所有对 Gowith 的显著变更都会记录在本文件。格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 修复

- **出行途中重启 App 后背包选择不再丢失**：完成清点后「拿东西」页正常显示当前背包与角标，物品库「已装入背包」计数恢复（此前会显示「还没有背包」「0 件」，需要手动重新选择背包；数据本身无损）。
- 出行中物品库资产卡副指标由误导性的「已装入背包 0 件」改为「携带中 N 件」，与实际一致；出行结束后恢复「已装入背包」。
- **货架行对 VoiceOver 可用**：读屏双击即可装入 / 移出背包，并提供「编辑物品」快捷动作（此前读屏用户无法完成装包操作）。
- 修正物品装入背包后，图标库图标在深色底上渲染为深色不可见的问题（深底自动转浅色）。

## [1.1] - 2026-09-25

稳定性与体验小版本：出行期间防误操作、图片体积优化、待确认提醒入口，并建立自动化测试。

### 修复

- **出行中清单锁定**：出行会话进行中（含回家清点阶段），物品库与拿东西页的装包 / 移除 / 编辑自动锁定并显示说明卡。此前人在出发地 50 米内仍可把已装物品移出背包，导致背包与本次出行清单不一致、物品在界面间"消失"。

### 新增

- **待确认提醒入口**：「我的」页顶部在有待确认物品时显示橙色卡片（N 件待确认 · 3 天内未确认将自动转遗失），点击直达历史记录补登。刻意不做弹窗式打扰。
- **自动化测试**：新增单元测试 target `GowithTests`，9 个用例覆盖出行到达状态机（回家清点 vs 异地检阅）、3 天自动转遗失（时间边界 / 遗失计数累加 / 孤儿物品归位）、货架归属与图片压缩规则，核心业务规则从此有回归护栏。

### 优化

- **照片体积**：物品 / 背包照片在保存前统一压缩到 1024px 长边（JPEG 0.82），相机原图可达 3–5MB/张，缩图后图片目录体积降一个数量级；小于 1024px 的图不放大。
- **真机回归清单**：新增 `docs/真机回归清单.md`，六个场景（发起出行 → 离开始发地 → 到达异地 → 回家清点 → 手动兜底 → 3 天过期）照着走即可验证围栏核心闭环。

### 移除

- 「我的 → 账号」的 iCloud 同步占位行（已决策不做 iCloud，避免 entitlements 破坏免费签名侧载路线）。

### 升级说明

- 数据结构无变化，v1.0 / v1.0.1 用户安装后数据原样可用。

## [1.0.1] - 2026-09-25

本版本是一次大规模界面与交互重构：App 从单一彩色模式升级为「基础 / 进阶」双模式运行时切换，视觉体系更换为黑白极简（唯一强调色橙 `#FF5A26`），并新增可搜索物品图标库。**数据模型零破坏迁移**，旧数据无需任何处理即可继续使用。

### 新增

- **基础 / 进阶双模式**：页面右上角胶囊开关随时切换（伴随触觉反馈）。基础模式浅色 4 Tab（物品库 / 拿东西 / 检查 / 我的），聚焦「添加物品 → 装包出行 → 回家清点」核心闭环；进阶模式整体转深色，扩展为 5 Tab 并解锁地图 / 地点与历史 / 统计。模式持久化于 `gowith.appMode`。
- **黑白视觉体系**：黑 `#111`、白、灰阶设计令牌组（自动适配浅 / 深色外观），全局唯一强调色橙 `#FF5A26`；新增资产卡片（双数字大卡）、图标化 Tab 栏、悬浮主按钮等组件。
- **首启教学动画**：首次启动播放三帧横滑引导（01 添加 → 02 装包 → 03 清点），可跳过，「我的」页可重看（`gowith.hasSeenTutorial`）。
- **物品图标库 `GowithIconLibrary`**：109 枚命名图标、13 个分类（包袋箱包 / 随身数码 / 交通住宿等），物品与背包编辑器共用带搜索框与分类筛选的选择器；SF Symbol 作占位，后续把素材按 id 放入 Assets 即全局自动替换（对照表见 `docs/icon-library.md`）。
- **装包飞球动画**：点「+」装入物品时，按钮旋转 45° 变 ✕、橙色小球飞入「拿东西」Tab 角标并弹跳，状态字转橙。
- **三合一添加按钮**：Tab 栏右侧常驻「+」悬浮按钮，菜单聚合添加物品 / 背包 / 地点三个入口。
- **设置项**：触觉反馈开关（`gowith.hapticsEnabled`）、离家自动记录开关（`gowith.exitRecordingEnabled`，已接入围栏监测）。

### 变更

- Tab 栏改为纯图标样式（21pt 图标，选中放大弹跳），底栏悬浮贴底、不再遮挡内容。
- 悬浮主按钮钉底于 Tab 栏上方，下方垫渐隐托底；待办与角标数量实时联动。
- 「拿东西」页整合背包选择与装包清单；「检查」页承接回家清点；原「出行会话」页按出行前 / 到达后拆分进两个 Tab。
- 文档：新增设计规格书 `docs/superpowers/specs/2026-09-25-core-mode-redesign-design.md`、iPhone 侧载安装教程 `docs/iPhone安装教程.md`、图标库对照表 `docs/icon-library.md`。

### 修复

- 修复装饰性描边 / 图层默认拦截触摸，导致资产卡徽标与部分行内按钮点不动的问题（`allowsHitTesting(false)`）。
- 修复深色底上 SF Symbol 图标颜色写死白色、未随主题反转的问题。
- 修复模式胶囊开关在辅助功能（VoiceOver）下无法点击的问题。
- 修复「+」添加菜单因 sheet 挂载丢失导致三个入口点击无反应的问题。
- 修复个别 SF Symbol 名称错误（`glasses` → `eyeglasses`），并校验全部占位符在 iOS 17+ 有效。

## [1.0.0] - 2026-09-24

首个公开发布版本，纯本地 MVP。

- 地点 / 背包 / 物品库 / 出行会话 / 历史记录五大模块，4 Tab 导航。
- 出行会话状态机：准备出门 → 出门中 → 到达 / 回家清点 → 完成；地理围栏（约 50 米）自动监测离开与到达并发送本地通知。
- 待确认物品 3 天宽限期，到期自动标记遗失并归位出发地。
- 纯本地 JSON 持久化 + 图片文件存储，零第三方依赖、零网络请求。
- 详情见 [README](README.md) 与[产品原型报告](Gowith-产品原型报告.md)。

[1.1]: https://github.com/wayne0612/Gowith/compare/v1.0.1...v1.1
[1.0.1]: https://github.com/wayne0612/Gowith/compare/v1.0.0...v1.0.1
[1.0.0]: https://github.com/wayne0612/Gowith/releases/tag/v1.0.0
