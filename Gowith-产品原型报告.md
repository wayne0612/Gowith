# Gowith 产品原型报告

版本：1.0
日期：2026-09-25
依据：Gowith iOS 客户端源码（10 个 Swift 文件，约 4000 行）+ 《Gowith-Design-System.md》v2.0（唯一生效设计规范）
平台：iOS 17+ / SwiftUI / 纯本地 MVP（无后端、无第三方依赖）

---

## 1. 产品定位

**一句话**：出行物品管理 App ——「带上要带的，记住带回的」。

Gowith 记录的是一个真实物品从某个家出发、跟着用户移动、在另一个地点被放下、最后被确认带回的空间关系。它不是待办清单，也不是电商商城，而是一条对象关系链：

```
地点 → 背包 → 物品 → 行程 → 新地点 → 回家清点
```

**目标场景**：
- 通勤 / 差旅 / 健身 / 摄影外出等需要带装备的场景
- 痛点一：出门忘带关键物品（钥匙、充电器、相机卡）
- 痛点二：物品放在外地（如健身房、办公室）后忘记带回
- 痛点三：带出去的东西回家后不确定是否带回了

**核心设计关键词**：空间感 · 对象优先 · 横向浏览 · 连续清单 · 色彩分区 · 轻动效 · 克制的高级感

---

## 2. 信息架构与导航

### 2.1 启动流程

```
首次启动 ──→ Hero 品牌页 ──→ 引导设置（4 步） ──→ 主界面
再次启动 ──────────────────────────────────→ 主界面
```

引导设置四步（GowithSetupView）：
1. 给第一个「家」命名，设置位置（GPS 定位 / 地图点选双通道）
2. 创建第一个背包（内置 8 个伪 3D 图标可选）
3. 添加第一件物品（如钥匙，6 种图标可选）
4. 授权定位与通知（说明用途，不强制）

### 2.2 应用外壳（App Shell）

- **主导航**：系统 TabView 四项底部导航，顺序固定：`背包 · 物品库 · 地图 · 我的`
- **出行态切换**：存在活动出行会话时，「地图」Tab 被出行会话页（GoView）替换 —— 出行是地图页的升维状态
- **顶部区域**：轻量品牌抬头 `Gowith` + 页面标题 + 当前地点上下文条（GowithContextStrip：地点名 · 背包名 · 距离/在范围内状态）
- **底部主操作**：固定于安全区底部，普通实色按钮；无悬浮球、无毛玻璃 Dock

### 2.3 页面清单

| 页面 | 文件 | 职责 |
| --- | --- | --- |
| Hero 品牌页 | RootView.swift | 首次启动品牌展示 |
| 引导设置 | RootView.swift | 创建家/背包/首件物品/授权 |
| 背包页（默认入口） | RootView.swift | 选择背包、查看已装物品 |
| 物品库 | ItemViews.swift | 按地点+分类管理物品、装包、发起出行 |
| 地图/地点页 | PlaceViews.swift | 地点地图、新建/编辑地点、地点库存 |
| 出行会话页 | GoView.swift | 出行全流程：出门→到达→清点 |
| 我的 | ProfileViews.swift | 统计、历史记录、会话详情 |
| 各类编辑器 | 各文件 | 背包/物品/分类/地点编辑器、相机拍照 |

---

## 3. 核心业务流程

### 3.1 出行会话状态机（产品的灵魂）

```
[选背包+装物品+在出发地50m内]
        │ 发起出行，确认「带上N件物品，开始出门」
        ▼
   ┌─────────┐   监听到离开家    ┌─────────┐
   │ PREPARING│ ──────────────→ │  AWAY   │（出门中）
   └─────────┘                  └────┬────┘
                                     │ 到达某地点（围栏触发/手动确认）
              ┌──────────────────────┴──────────────┐
              │ 回到出发地                            │ 到达其他地点
              ▼                                      ▼
        ┌──────────┐   开始检阅    ┌──────────┐  选择放入/继续携带
        │ CHECKING │              │ ARRIVED  │ ──────→ 存放
        │（回家清点）│              └──────────┘
        └────┬─────┘
             │ 逐件确认带回
             ▼
        ┌──────────┐
        │COMPLETED │（未确认项转「待确认」）
        └──────────┘
```

**关键规则**：
- **出发门槛**：必须在出发地约 50 米范围内 + 已选背包 + 背包内有物品，否则禁用并提示
- **地理围栏**：出发后监听「离开家」事件 → 武装所有其他地点的进入围栏 → 到达触发本地通知
- **回家即清点**：回到家同样触发围栏，直接进入清点模式

### 3.2 物品状态机

```
                    出门
     [地点库存物品] ────→ UNCONFIRMED（出门清单）
                        │ 回家清点
            ┌───────────┼──────────────┐
            ▼           ▼              ▼
        RETURNED     PENDING        （保留携带）
        （已带回）  （待确认，3天期限）
                        │ 超过3天
                        ▼
                      LOST（遗失）
                        │ 历史记录中可补确认
                        ▼
              RETURNED / 维持 LOST

     到达异地检阅：SELECTED → STORED（放入该地点）/ CARRIED（继续携带）
```

- **待确认 3 天规则**：回家未逐件确认的物品进入「待确认」状态，显示明确截止日期；逾期自动转「遗失」并累计遗失次数；期限内可在「我的 → 历史记录」中补确认
- **位置门槛**：物品的取放（装包/取出/放入地点）均要求身处对应地点约 50 米范围内 —— 这是「空间关系」的产品化表达

### 3.3 一屏一焦点 / 一页一主操作

| 状态 | 页面唯一主操作 |
| --- | --- |
| 未选择背包 | 选择背包 |
| 已选背包 | 选择物品 |
| 物品已装入 | 开始出行 |
| 出行中 | 查看地图 / 确认到达 |
| 到达地点 | 开始检阅 |
| 检阅中 | 完成放入 / 完成清点 |
| 清点有未确认 | 继续完成或返回核对 |
| 行程完成 | 查看结果 / 开始下一次 |

---

## 4. 逐页面原型说明

### 4.1 背包页（默认入口）

- **主焦点**：当前背包 —— 大面积场景色卡片 + 伪 3D 图标/实拍图 + 倒影
- **交互**：左右滑动切换背包（对象轨道 GowithObjectRail：前后对象缩小+模糊露出边缘，形成空间层级）；下方位置指示点（≤3 个背包逐点对应，>3 个收敛为头/中/尾）
- **主操作**：「选择物品」→ 跳转物品库
- **其他**：已装物品连续列表（行高 68pt，勾选状态切换）、长按删除背包（被活动会话引用时禁用）、右上角添加背包
- **空状态**：3D 图标 + 「创建第一个背包」引导

### 4.2 物品库

- **结构**：顶部统计摘要（总数/已装包数）→ 横向分类轨道（GowithCategoryRail，选中项胶囊高亮）→ 连续物品列表 → 底部「开始出行」主按钮
- **行结构**：图标/缩略图（44pt 稳定位置）+ 名称 + 分类/状态说明 + 右侧装包圆形勾选
- **交互**：点击整行装/卸包（超出范围时提示需到达 50m 内）；侧滑删除；相机拍照作为物品图
- **分类管理**：分类编辑器支持增删改、拖拽排序；删除分类自动归入「未分类」
- **门槛**：添加物品 / 开始出行均要求在当前地点范围内

### 4.3 地图 / 地点页

- **主焦点**：地图（MapKit）+ 地点标记（家 = house.fill，其他 = mappin）
- **每个地点 50m 围栏圆环可视化**
- **交互**：点标记 → 地点库存弹层（物品数/背包数/距离，「去这里拿物品」直达物品库）；新建地点（地图点选/当前 GPS，含 50m 提醒范围说明）；编辑地点（改名/改坐标/删除 —— 非空地点不可删）
- **出行态**：此页整体被出行会话页替换

### 4.4 出行会话页（GoView）

按会话状态呈现完全不同的内容，但保持同一页面骨架（状态场景 + 统计行 + 清单 + 底部主操作）：

| 会话状态 | 界面内容 | 主操作 |
| --- | --- | --- |
| 出门中 | 色彩场景卡（背包名·物品数·出发时间）+ 出门清单 + 地图 | 确认到达（选择目的地） |
| 已到达异地 | 到达摘要 + 「开始检阅」引导 | 开始检阅 |
| 检阅中 | 物品逐件勾选（放入地点 / 继续携带） | 完成放入 |
| 回家清点 | 逐件确认带回（带回/待确认/遗失三态） | 完成清点 |
| 有未确认项 | 拦截提示：N 件未确认 | 继续完成 / 带着待确认结束 |

### 4.5 我的

- 统计卡：总物品数 / 进行中行程 / 待确认数 / 累计遗失
- 地点库存分组总览（每地点的物品与背包数）
- 历史记录列表（按时间倒序，含行程摘要）→ 会话详情（逐件物品快照、状态徽章、待确认项补确认入口）
- 设置项：通知开关直达系统设置

---

## 5. 设计系统摘要

### 5.1 视觉语言

**基础层**（温暖、明亮、低噪声画布）：

| Token | 颜色 | 用途 |
| --- | --- | --- |
| canvas | #F5F3F0 | 页面背景 |
| surface | #FFFFFF | 列表和表单 |
| ink | #111111 | 主文字/主操作 |
| inkSecondary | #6F6D69 | 辅助文字 |
| line | #E2E0DC | 分隔线 |

**场景色**（一屏仅一个主场景色，颜色不替代语义）：

| Token | 颜色 | 对象 |
| --- | --- | --- |
| sceneMint | #BCEFE0 | 当前地点、已完成 |
| sceneCoral | #F26A68 | 遗失（必须配文字+警示图标） |
| sceneAmber | #F7C86B | 待确认 |
| sceneSky | #A9D9F5 | 地图、远程地点 |
| sceneOrange/Violet/Blue | — | 数码/摄影/学习类主题 |

深色模式全量自适应；ReduceMotion 全局关闭装饰动效。

### 5.2 造型与排版

- 圆角三级：主场景 28pt / 小容器 16pt / 按钮输入 14–16pt；同层不超过两种圆角
- 4pt 间距基准；触控目标 ≥44×44pt
- SF Pro 系统字体 7 级层级（Brand/Screen/Object/Section/Body/Meta/Micro）；支持动态字体，布局允许垂直增长

### 5.3 组件清单（DesignSystem.swift）

`GowithCard`（场景容器）· `GowithContextStrip`（地点上下文条）· `GowithObjectRail`（对象横向轨道）· `GowithFocusField`（聚焦卡）· `GowithPrimaryButton/SecondaryButton` · `GowithInventoryRow`（库存行）· `GowithStatusBadge`（状态徽章）· `GowithCategoryRail`（分类轨道）· `GowithEmptyState` · `GowithBackpackPreview` · `GowithTopBar` · `GowithHeroView`

### 5.4 动效语法

- 状态反馈 150–220ms，层级切换 180–280ms，连续运动 ≤350ms
- 全部由交互触发（点击/滑动/到达事件），无自动轮播、无循环装饰
- 反馈发生在操作附近（行内图标/勾选绘制），不整页重排
- 动画之外静态状态完整可读（状态 = 图标+文字双表达，VoiceOver 标签齐全）

---

## 6. 数据模型

```
GowithPlace（地点：名称/坐标/半径50m）
GowithBackpack（背包：名称/图标或图片/所属地点/物品ID列表）
GowithCategory（分类：名称/图标/排序）
GowithItem（物品：名称/图标或图片/分类/所属地点/遗失计数）
OutingSession（出行会话：起止时间/出发地/目的地/背包快照/状态/离开出发地标记）
SessionItem（会话物品快照：物品ID+名称/图标快照/选中态/状态/时间戳）
```

- **持久化**：JSON 文件（Application Support/gowith-data.json）+ 图片文件目录；模型手写 Codable，会话记录物品快照保证历史可读
- **服务**：CoreLocation（持续定位 + CLCircularRegion 围栏监测）+ UserNotifications（到达/回家本地通知）

---

## 7. 可访问性

- 状态永不单靠颜色/动画表达：图标+文字双通道
- 全量 accessibilityLabel（中文语义化），导航圆点/统计行合并读出
- 触控目标 ≥44pt；动态字体支持；ReduceMotion 降级

---

## 8. 质量状态与建议

### 已完成（2026-09-25 深度测试轮次）

修复：孤儿物品（待确认/遗失物品地点悬空导致不可见）、首次定位授权后卡死、定位精度与 50m 判定不匹配、地点无法删除、背包指示点数量错误、到达状态统计错误；优化：图片内存缓存（NSCache）、死代码清理（5 视图 + 死状态）、到达逻辑统一、设置页地图选点回退。

### 待办建议（按优先级）

1. **真机回归**：地理围栏全流程（离开家→到达异地→回家清点）必须真机验证，模拟器区域事件不可靠
2. **单元测试**：GowithStore 状态机（pending 3 天过期、地点结算）是纯逻辑，建议补 XCTest target
3. **Widget / 快捷指令**：「开始出门」入口前置到锁屏/桌面，缩短发起路径
4. **多背包出行**：当前一次出行只带一个背包
5. **数据云端同步**：换机/多设备场景（当前纯本地）

---

*本报告由源码与设计规范反向工程生成，可作为 PRD 依据、新成员上手材料或后续迭代的基线文档。*
