# Gowith UI System（已废弃）

> 本文件不再作为开发规范。Gowith 已完成一次设计系统重构，请使用唯一生效文档：[Gowith-Design-System.md](./Gowith-Design-System.md)。
>
> 以下内容保留为历史记录，不应继续用于新页面、组件或设计 Token。

版本：1.0  
适用平台：iOS 17+ / SwiftUI  
状态：第二轮 UI 已实施；动态字体与深色模式仍需完整验收

这份文档是 Gowith 当前 iOS UI 的执行规范。它将早期的 Web/Desktop 设计要求转换为适合单手操作、连续清点和系统可访问性的 iOS 方案。原有参考文稿仍作为视觉背景参考，但本文件优先级更高。

## 1. 设计依据

本规范参考 Apple Human Interface Guidelines 及 SwiftUI 官方文档：

- [Human Interface Guidelines](https://developer.apple.com/design/human-interface-guidelines/)
- [Layout](https://developer.apple.com/design/human-interface-guidelines/layout)
- [Materials](https://developer.apple.com/design/human-interface-guidelines/materials)
- [Buttons](https://developer.apple.com/design/human-interface-guidelines/buttons)
- [Tab bars](https://developer.apple.com/design/human-interface-guidelines/tab-bars)
- [Accessibility](https://developer.apple.com/design/human-interface-guidelines/accessibility)
- [SwiftUI Documentation](https://developer.apple.com/documentation/swiftui)

借鉴原则：

1. 使用系统语义字体、动态字体和系统控件行为，让界面适应不同设备和文字大小。
2. 使用安全区、系统 Tab bar、NavigationStack 和标准 sheet 管理页面结构与层级。
3. 把主要操作放在用户当前任务附近，避免让用户依赖悬浮按钮或隐藏手势。
4. 让状态同时通过图标、文字、位置和控件状态表达，颜色只作为辅助。
5. 保持触控目标至少 44 × 44 pt，并为 VoiceOver 提供清晰的标签和操作描述。
6. 只在内容需要层级分离时使用容器，避免把每个列表项都做成孤立的大卡片。

## 2. 产品体验目标

Gowith 的核心任务是：用户选择背包，选择要带出的物品，开始一次出行，回家后快速清点。

界面优先级固定为：

1. 当前任务和唯一主要操作。
2. 当前背包或本次出行的核心内容。
3. 物品状态和数量。
4. 编辑、删除、历史和设置等辅助操作。

每个页面首屏都应回答两个问题：

- 我现在处于哪一步？
- 下一步最应该做什么？

页面应保持连续的纵向内容流。标题、统计、分类和列表通过留白与细分隔线建立层次，减少嵌套卡片和重复圆角。

## 3. 已确认的视觉决策

### 3.1 移除顶部黑色主题卡片

页面顶部使用标准导航标题或内联标题，不再使用黑色悬浮主题卡片承载“物品库”、数量和 Logo。

- 标题使用 `.navigationTitle` 或页面内的 `title2`。
- 数量作为标题下方的辅助信息，使用 `subheadline` 或 `footnote`。
- Logo 只在品牌入口、空状态或关于页面出现，不抢占任务区域。
- 顶部不放与当前任务无关的装饰性大容器。

### 3.2 移除毛玻璃底部 Dock 和悬浮 GO

底部导航使用系统 `TabView` 的标准 Tab bar，四个入口依次为：

1. 背包
2. 物品库
3. 地图
4. 我的

Tab bar 只显示 SF Symbols 图标，图标需保留对应的 VoiceOver 名称。
底部导航只负责页面切换，不承载出行操作，也不使用自定义悬浮容器、毛玻璃 Dock 或悬浮 GO 圆钮。

“开始出行”是当前流程中的主要操作，在物品库页面固定于系统 Tab bar 上方，使用标准主按钮，便于长清单中随时操作。地图页在 MVP 阶段可以继续作为占位页；存在活动出行时，该入口承载本次出行与清点内容。

### 3.3 采用连续的 Apple 风格列表

列表是 Gowith 的主要内容结构。物品库和清点页使用连续的白色列表区域或 `List`，单个物品使用行级布局，不为每一行建立大卡片。

- 行高建议 64–76 pt，点击区域覆盖整行。
- 使用轻量分隔线或背景分组区分内容。
- 图片或 SF Symbol 固定为 44–52 pt，保持位置稳定。
- 右侧只放当前状态或一个主要操作。
- 编辑、删除等低频操作通过 swipe action、toolbar 或 sheet 提供。
- 卡片只保留给空状态、确认摘要、重要完成状态和需要聚焦的单一任务。

## 4. 颜色系统

颜色采用暖白背景、白色内容面和近黑色文字。颜色用于表达层级和状态，避免把页面做成单一的彩色主题。

SwiftUI 语义 Token：

```swift
appBackground   = #F7F6F3
surface         = #FFFFFF
surfaceSubtle   = #F1F0ED
textPrimary     = #161616
textSecondary   = #6F6F6F
textTertiary    = #969696
separator       = #E5E3DF
accent          = #161616
```

状态色只做低饱和背景辅助：

| 状态 | 主要表达 | 辅助背景 |
| --- | --- | --- |
| 未携带 | 图标 + “未携带” | `surfaceSubtle` |
| 已携带 | 勾选图标 + “已携带” | 轻微中性灰 |
| 已带回 | 勾选图标 + “已带回” | 低饱和绿色灰 |
| 待确认 | 时钟图标 + “待确认” + 截止日期 | 低饱和暖灰 |
| 遗失 | 警示图标 + “遗失” | 低饱和红灰 |

任何状态都不能只依赖颜色区分。

待确认期限默认从回家清点开始计算 3 天。期限内用户可以在历史记录中补充确认；界面需要显示明确的截止日期，过期后再按产品规则处理，不使用模糊的“稍后处理”文案。

## 5. 字体与排版

优先使用 SwiftUI 语义字体，不写死字号，使动态字体生效：

| 用途 | SwiftUI 样式 |
| --- | --- |
| 页面标题 | `.title2` 或 `.largeTitle` |
| 区块标题 | `.headline` |
| 物品名称 | `.body` |
| 辅助说明 | `.subheadline` / `.footnote` |
| 状态和数量 | `.caption` / `.caption2` |

文字颜色分三层：主标题使用 `textPrimary`，说明文字使用 `textSecondary`，时间、数量和辅助元数据使用 `textTertiary`。

页面内避免同时出现超过四种明显字号。标题使用系统无衬线字体，不额外引入展示字体，也不使用全大写作为主要信息层级。

## 6. 间距、尺寸和圆角

使用 4 pt 基础间距，常用值为 8、12、16、20、24、32。

| 场景 | 规范 |
| --- | ---: |
| 页面水平边距 | 20 pt，紧凑场景可用 16 pt |
| 区块间距 | 24 pt |
| 列表行内边距 | 16 pt |
| 列表行高 | 64–76 pt |
| 主要按钮高度 | 50–52 pt |
| 最小触控区域 | 44 × 44 pt |
| 图片或图标容器 | 44–52 pt |
| 输入框圆角 | 12–16 pt |
| 聚焦容器圆角 | 20–24 pt |

列表不使用连续的大圆角卡片。必要的容器最多使用一层圆角，避免“卡片套卡片”。阴影默认关闭，优先用背景差异、分隔线和留白建立层级。

## 7. Material 使用规则

毛玻璃材料不再作为底部 Dock、悬浮 GO 或常规列表项的默认背景。

只有以下系统场景可以使用 Material：

- 系统菜单、popover、sheet 等临时浮层。
- 需要与背景内容保持分离的系统级覆盖层。
- 明确需要体现浮层关系的确认面板。

普通页面使用 `appBackground`，内容区使用 `surface` 或 `surfaceSubtle`。材料不能替代结构、对比度或按钮状态。

## 8. 页面规范

### 8.1 背包页

背包页是默认入口，任务是让用户选择出行使用的背包。

- 使用标准导航标题“背包”。
- 标题下显示背包总数等简短统计，不放黑色主题卡片。
- 背包主体可以保留左右滑动选择，但内容应融入连续页面，不使用过度装饰的卡片外框。
- 图片、背包名称和编辑入口保持固定布局。
- 三点只作为位置提示，放在主体内容下方。
- “选择物品”是页面唯一主要按钮，放在背包选择完成后的近邻位置。
- 没有背包时提供空状态和“创建背包”动作。

### 8.2 物品库

物品库用于筛选、查看和加入当前选中的背包。

- 使用标准导航标题“物品库”，数量作为辅助信息。
- 分类栏使用横向滚动的紧凑选择控件，图标与文字同层展示；分类名称最多四个字且不换行。
- 分类栏与物品列表之间保持 16–24 pt 间距。
- 物品使用连续列表行，左侧为缩略图或 SF Symbol，中间为名称和分类信息，右侧为加入/移除状态。
- 抓手或加入动作应有明确的可访问标签，如“加入当前背包”“从当前背包移除”。
- 已加入物品通过图标、文字和行状态表达；不依赖灰色单一表达。
- 添加物品、编辑物品、分类管理使用 sheet 或 toolbar action。
- 没有物品时提供空状态和“添加物品”动作。

### 8.3 地图页

地图页是底部第三个入口。MVP 阶段可以显示清晰的占位状态，不伪装成已完成的定位功能。

- 使用标准导航标题“地图”。
- 占位状态说明未来用于查看出行位置和记录。
- 地图功能完成前，不放虚假的定位数据或不可用的地图控件。

### 8.4 GO 与出行清点

GO 不是独立的底部导航入口。开始出行由背包页或物品选择流程中的主要按钮触发，之后进入出行清单页。

- 出行开始前显示背包名称、物品数量和物品摘要。
- 主按钮使用明确的动词，例如“开始出行”“我回来了”“完成清点”。
- 清点时整行可点击，已带回、待确认和遗失同时显示图标与文字。
- 存在待确认项时，确认浮层说明数量、截止时间和后续影响。
- “继续完成”和“返回核对”应是两个清晰、可区分的操作。
- 完成状态使用简洁插图或图形作为辅助，不影响下一步动作。

### 8.5 我的

“我的”使用标准设置列表，不使用账号资料卡或装饰性大卡片。

- 统计使用简洁的分组行，显示总出门次数、完成清点次数和遗失数量。
- 偏好设置采用标准 Form/List 行，包括待确认期限、主题和数据管理。
- 关于 Gowith 作为单独分组。

## 9. 组件职责

组件只负责一种稳定的视觉或交互职责，页面负责组合这些组件。

基础组件：

- `GowithPrimaryButton`：页面唯一主要动作，支持图标、禁用和加载状态。
- `GowithSecondaryButton`：次要动作，保持低视觉权重。
- `GowithItemRow`：物品缩略图、名称、辅助信息和状态/动作。
- `GowithStatusBadge`：图标、文字和可选截止时间。
- `GowithImageThumbnail`：统一图片或 SF Symbol 尺寸、裁剪和无障碍描述。
- `GowithSectionHeader`：区块标题与简短统计。
- `GowithEmptyState`：插图、说明和一个下一步动作。

页面组件：

- `BackpackView`：背包选择和进入物品选择的流程。
- `ItemLibraryView`：分类筛选和物品加入/移除。
- `GoView`：出行、返回和清点状态。
- `MapPlaceholderView`：地图页占位或后续地图内容。
- `ProfileView`：统计、偏好和关于。

旧的 `GowithBottomBar`、`FloatingGoButton` 以及将 Material 直接作为常规内容背景的写法，不作为第二轮 UI 优化的目标组件。

## 10. 交互和动效

交互反馈应让用户知道操作已经生效：

- 加入或移除物品时更新整行状态，并提供轻量按压反馈。
- 背包左右切换使用短促、可中断的系统动画。
- 保存表单后关闭 sheet，回到原列表并保持原来的分类和滚动位置。
- 删除背包、物品或分类前使用系统确认对话框。
- 主要动作完成后保持页面上下文，不把用户送到无关页面。
- 动效时间控制在约 150–220 ms；支持减少动态效果设置。

不使用持续动画、自动轮播、依赖悬停的反馈或必须记忆的隐藏手势。

## 11. 无障碍和响应式要求

- 所有按钮和可点击行的实际触控区域至少为 44 × 44 pt。
- 使用 `accessibilityLabel` 和 `accessibilityHint` 描述图标按钮与抓手动作。
- Dynamic Type 放大后，文字可以换行，不能被裁切或覆盖。
- 状态必须包含可读文本，颜色不能承担唯一语义。
- 图片使用有意义的描述；纯装饰图片标记为隐藏。
- 竖屏下不得出现横向内容溢出；分类栏可以横向滚动。
- 在紧凑宽度下，物品行应优先保留名称、状态和主要动作，次要元数据可以隐藏。
- 支持浅色和深色外观时，颜色 Token 需要提供语义化的明暗版本。

## 12. 第二轮实施记录

1. 自定义底部 Dock 已替换为系统 `TabView`，包含背包、物品库、地图、我的四个入口；可见标签按产品要求隐藏，VoiceOver 名称保留。
2. 悬浮 GO 已移除；“开始出行”位于物品库的固定主操作区，清单可滚动且无需回到列表底部。
3. 顶部黑色主题卡片已移除，改用系统导航标题和轻量统计信息。
4. 物品库与清点内容已调整为连续行式列表，使用细分隔线。
5. `DesignSystem.swift` 已集中语义色、间距、圆角和按钮规范，并为浅色/深色外观提供颜色适配。
6. 物品加入/移除、Tab 图标和清点状态已提供可读的无障碍名称或文字状态。
7. iPhone 17 浅色模式下背包、物品库、清点页和“我的”已完成模拟器目视检查；动态字体与深色模式仍待完整验收。

## 13. UI 验收标准

完成第二轮后，以下条件必须全部满足：

- 首屏能看出当前选中的背包和下一步动作。
- 页面顶部没有黑色主题悬浮卡片。
- 底部是系统风格的四项 Tab bar：背包、物品库、地图、我的。
- Tab bar 只显示图标，VoiceOver 能读出四个入口名称。
- 页面没有毛玻璃悬浮 Dock 和悬浮 GO 圆钮。
- 物品库的“开始出行”在滚动清单时仍可操作，且位于系统 Tab bar 上方。
- 物品列表是连续、可单手连续点击的行式结构。
- 分类栏可以横向滑动，图标与文字不换行。
- 已携带、已带回、待确认和遗失不依赖颜色即可区分。
- 所有主要动作的点击区域至少为 44 × 44 pt。
- 动态字体放大后没有文字裁切、横向滚动或内容重叠。
- 空状态、确认状态和完成状态都提供清晰的下一步动作。
- iPhone 竖屏下背包页、物品库、GO 清点页和我的页面视觉 Token 一致。
