# Gowith Design System

版本：2.0  
状态：新的唯一生效规范  
平台：iOS 17+ / SwiftUI

> 本文件取代 `UI-system.md` 与 `uiuxmotion-design.md` 中的全部视觉、布局、组件和动效规则。旧文件只保留为历史记录，不再作为开发依据。

## 0. 重构目标

Gowith 记录的是一个真实物品从某个家出发，跟着用户移动，在另一个家被放下，最后被确认带回的空间关系。它不是普通待办清单，也不是电商商城。

新的设计系统围绕这条关系建立：

```text
地点 -> 背包 -> 物品 -> 行程 -> 新地点
```

任何页面都必须让用户快速看懂：

1. 现在在哪个地点。
2. 当前选择了哪个背包、带着哪些物品。
3. 下一步应该执行什么动作。

## 1. 设计方向

### 1.1 核心关键词

```text
空间感 · 对象优先 · 横向浏览 · 连续清单 · 色彩分区 · 轻动效 · 克制的高级感
```

### 1.2 从参考视频借鉴什么

参考视频展示的是鞋类浏览页面。Gowith 不复制其中的商品、价格或商城语义，只借鉴可以迁移的交互语法：

- 页面先突出一个当前对象，而不是同时展示所有信息。
- 当前对象使用大面积色彩场景建立视觉焦点。
- 左右滑动用于切换同一类对象，前后对象露出一部分形成空间层级。
- 分类使用横向滚动轨道，选中项清楚但不喧宾夺主。
- 详细内容使用连续的行式列表，方便快速扫描和连续操作。
- 对象图片可以轻微突破色彩场景边界，产生悬浮感。
- 点击、滑动和选择都带来短促的局部反馈，而不是整页剧烈变化。

### 1.3 明确不借鉴什么

- 不把 Gowith 做成电商首页。
- 不使用价格、促销、排行榜或商品收藏等无关结构。
- 不让大图、3D 图标或彩色卡片遮挡物品名称和状态。
- 不用颜色替代“已携带、待确认、遗失”等语义。
- 不把动画做成持续播放的背景效果。

## 2. 产品体验原则

### 2.1 一屏一个主焦点

每个页面首屏只能有一个视觉主角：

- 背包页：当前背包。
- 物品库：当前背包与当前分类下的重点物品。
- 地图页：当前地点和地点之间的关系。
- 出行页：当前位置与当前行程。
- 我的：当前状态和地点库存总览。

其他内容退居为列表、辅助数据或低对比度信息。

### 2.2 一次只推动一个主任务

页面最多保留一个主要操作。主要操作必须是动词，并且与当前状态一致：

| 状态 | 主要操作 |
| --- | --- |
| 未选择背包 | 选择背包 |
| 已选择背包 | 选择物品 |
| 物品已装入 | 开始出行 |
| 出行中 | 查看地图 |
| 到达地点 | 开始检阅 |
| 检阅中 | 完成放入目的地 |
| 回到出发地 | 完成清点 |
| 清点存在未确认 | 继续完成或返回核对 |
| 行程完成 | 查看结果或开始下一次 |

### 2.3 对象关系优先于页面关系

用户不是在页面之间无目的地跳转，而是在操作对象之间移动：

```text
地点中的背包
  -> 选择
背包中的物品
  -> 带出
行程中的物品
  -> 到达
目的地中的物品
  -> 回家清点
已带回 / 待确认 / 遗失
```

每次页面切换都要保留上一个对象的视觉线索，例如背包缩略图、地点名称、物品数量或地图标记。

### 2.4 状态不能只靠动画和颜色

所有状态都至少使用两种表达：图标+文字、文字+数量、文字+截止日期，或位置+操作状态。亮青绿色、琥珀色和红色只负责辅助建立优先级。

## 3. 应用外壳和导航

### 3.1 主导航

使用四项底部导航，顺序固定：

```text
背包 · 物品库 · 地图 · 我的
```

视觉上只显示图标，VoiceOver 必须读出中文名称。使用系统 `TabView` 行为，不重新制作毛玻璃 Dock、悬浮胶囊导航或悬浮 GO 按钮。

导航只负责切换上下文，不承担“开始出行”这种业务操作。出行由当前页面的主操作触发。

### 3.2 顶部区域

顶部采用轻量品牌抬头：

```text
Gowith                         当前地点 / 操作
页面标题或当前对象             辅助信息
```

规则：

- `Gowith` 位于左上，首字母 `G` 大写。
- 标题使用 SF Pro 系统无衬线字体。
- 品牌名明显但克制，不使用顶部黑色主题卡片。
- 当前地点始终可见，但不使用大块黑色按钮抢占焦点。
- 页面操作使用右上角至少 44pt 的图标按钮。

### 3.3 底部主操作

主要操作固定在当前页面内容的安全区底部，使用普通实色按钮或系统按钮样式。

- 不覆盖列表内容。
- 不做成永久悬浮球。
- 列表滚动时仍然可用。
- 用户开启“减少动态效果”时仍保持完整功能。

## 4. 视觉语言

### 4.1 基础层

Gowith 的基础层是温暖、明亮、低噪声的画布：

| Token | 颜色 | 用途 |
| --- | --- | --- |
| `canvas` | `#F5F3F0` | 页面背景 |
| `surface` | `#FFFFFF` | 连续列表和表单内容 |
| `surfaceMuted` | `#ECEBE8` | 次级行、未选中区域 |
| `ink` | `#111111` | 主文字和主操作 |
| `inkSecondary` | `#6F6D69` | 辅助文字 |
| `inkTertiary` | `#A19E99` | 次要数量和时间 |
| `line` | `#E2E0DC` | 分隔线 |

### 4.2 彩色场景

彩色不是全局主题背景，而是当前对象的“场景色”。一个页面同一时刻只允许一个主要场景色，其他颜色退为小面积点缀。

| 场景 Token | 建议颜色 | 适用对象 |
| --- | --- | --- |
| `sceneMint` | `#BCEFE0` | 当前地点、已完成、常用背包 |
| `sceneCoral` | `#F26A68` | 遗失、需要注意的异常对象 |
| `sceneAmber` | `#F7C86B` | 待确认、即将到期 |
| `sceneSky` | `#A9D9F5` | 地图、远程地点、路线 |
| `sceneViolet` | `#C9B8F5` | 摄影、创作类主题 |
| `sceneOrange` | `#F2A66D` | 数码和高频使用物品 |
| `sceneBlue` | `#AEBDEB` | 学习、办公类主题 |

场景色使用规则：

- 大面积场景只承载一个对象和少量对象信息。
- 场景内文字必须通过对比度检查。
- `sceneCoral` 不自动等同于“遗失”，必须同时显示文字和警示图标。
- 主题可替换场景色，但状态语义色必须保持独立。

### 4.3 阴影、边界和圆角

- 默认不使用重阴影。
- 主要场景圆角：28pt。
- 小型内容容器圆角：16pt。
- 输入和按钮圆角：14–16pt。
- 列表本身不拆成大量独立卡片。
- 同一层级不同时出现超过两种圆角。

## 5. 字体与排版

默认使用系统字体 `SF Pro`。品牌字标可以使用 `SF Pro Rounded` 粗体变体，但不引入外部字体。

| 层级 | SwiftUI 建议 | 作用 |
| --- | --- | --- |
| Brand | `.title3.weight(.bold)` | `Gowith` 抬头 |
| Screen | `.title2.weight(.semibold)` | 页面标题 |
| Object | `.title.weight(.bold)` | 当前背包/重点物品 |
| Section | `.headline.weight(.semibold)` | 区域标题 |
| Body | `.body` | 物品名称和主说明 |
| Meta | `.footnote` / `.caption` | 数量、地点、日期 |
| Micro | `.caption2.weight(.semibold)` | 极短状态标记 |

文字规则：

- 不使用负字距制造所谓高级感。
- 不用全大写作为主要层级。
- 标题可以换行，不能强制截断核心地点名或物品名。
- 单屏明显字号不超过五种。
- 动态字体放大时，布局允许垂直增长。

## 6. 核心组件系统

### 6.1 `GowithBrandHeader`

显示 `Gowith`、页面标题、当前地点和右上角操作。不负责统计、主按钮、地点库存和大面积背景。

### 6.2 `GowithFocusField`

承载当前页面唯一的视觉主角：

```text
场景色背景
对象标题
对象辅助数据
3D 图标 / 用户图片
可选的短状态
```

只承载一个对象。建议高度 250–330pt；对象图像可以越界 8–24pt，但不能压住文字；同一场景最多三个短操作。

### 6.3 `GowithObjectRail`

用于横向切换同一类对象，如背包或重点物品：

- 当前对象占主要宽度。
- 前后对象露出约 12–28pt。
- 当前对象缩放为 1.0，邻近对象为 0.82–0.9。
- 邻近对象降低透明度并增加轻微模糊。
- 支持拖拽和点击邻近对象。
- 不显示页码；需要位置提示时使用最多三个状态点或语义标签。

### 6.4 `GowithCategoryRail`

用于筛选物品分类：

- 横向滚动。
- 每个分类固定高度，最多四个汉字，不换行。
- 图标和文字同层显示。
- 当前分类变化时不改变控件尺寸。
- 分类切换不重置物品库滚动位置，除非结果为空。

### 6.5 `GowithInventoryRow`

固定顺序：

```text
缩略图 / SF Symbol -> 名称与辅助信息 -> 状态 -> 单一动作
```

最小高度 68pt；缩略图固定 48pt、圆角 14pt；整行点击区域至少 44pt 高；右侧只能有一个主操作。

### 6.6 `GowithStateMark`

统一显示状态图标、文字、数量或截止时间：

```text
✓ 已携带
◷ 待确认 · 9 月 27 日前
! 遗失
⌂ 已放在南宁
```

颜色只作背景或图标辅助，不省略文字。

### 6.7 `GowithBottomAction`

承载当前页面唯一主要动作。高度 50–54pt，水平边距 20pt，只显示一个动作。状态改变时可以更换标题，但保留位置并使用平滑过渡。

### 6.8 `GowithPlaceSheet`

地图点击地点后从底部出现：

```text
地点名称 + 距离/范围状态
背包库摘要
物品库摘要
允许的地点动作
```

使用系统 sheet detent，不使用左右弹窗作为唯一入口。

### 6.9 `GowithStatusScene`

展示到达、完成、空状态等阶段性状态。可以使用场景色和 3D 图标，但必须包含清楚的状态标题和下一步动作。

## 7. 页面和流程规范

### 7.1 首次使用

```text
Hero -> 填写第一个地点 -> 设置地点位置 -> 创建常用背包
    -> 添加至少一件常用物品 -> 进入背包首页
```

首次设置使用连续表单，不把每一步拆成多个漂浮卡片。完成前必须告诉用户还缺少什么。

### 7.2 背包页

背包页是默认入口，也是出门前的准备中心：

```text
Gowith                           当前地点
背包
当前背包总数 / 地点状态
横向背包对象轨道
当前背包名称 · 物品数量 · 准备状态
已装入物品
连续物品列表
选择物品 / 开始出行
```

规则：

- 当前背包使用 `GowithFocusField` 或 `GowithObjectRail`。
- 当前背包最大，左右对象只露出一部分。
- 3D 图标只用于当前对象和空状态。
- 当前地点必须显示在品牌抬头或对象辅助信息中。
- “选择物品”在没有出行时是主动作；物品准备好后变为“开始出行”。
- 只能在用户处于地点范围内时调整该地点库存。

### 7.3 物品库页

```text
Gowith                           当前地点
物品库 · N 件
当前背包：通勤包
全部  日用品  摄影  数码  学习
重点物品场景（可选）
物品连续列表
开始出行
```

分类轨道与当前背包语境保持可见。点击右侧抓手后，立即更新行状态和背包数量；可使用轻量位移动效，但不能等待动画结束后才保存状态。用户不在地点范围内时仍可浏览，加入/移除动作需要显示锁定原因。

### 7.4 地图页

地图只表达地点和移动关系，不展示过多周边 POI：

```text
Gowith                           添加地点
地图
最小化地图
地点房屋标记 · 当前定位 · 约 50 米范围
点击地点 -> GowithPlaceSheet
```

多个远距离地点存在时才使用中国范围全览；单地点自动聚焦。点击地点打开底部面板，展示背包库和物品库。定位不可用时始终提供手动确认入口。

### 7.5 出行页

出行页是状态型页面，不是第四个导航入口。

出行中地图优先，显示当前背包、携带数量和定位状态。到达其他地点后只显示：

```text
已到达「南宁」
地图 / 地点上下文
底部：开始检阅
```

点击“开始检阅”后才展示物品列表，底部主操作变为“完成放入目的地”。回到出发地后进入“回家清点”，每件物品默认是“待确认”，点击右侧圆点切换为“已带回”。

存在未确认项时，弹窗显示数量和截止日期；“继续完成”保留待确认状态，不计入已带回，也不立即标记遗失。默认期限为 3 天。

### 7.6 我的页

“我的”是统一数据看板，不是账号主页：

```text
品牌与页面标题
当前状态摘要
地点与库存总览
出行统计
偏好设置
数据与关于
```

地点总览使用可展开的连续行，不堆叠多层卡片。每个地点显示名称、当前范围状态、背包数量、物品数量，以及展开后的背包库和物品库。

## 8. 业务状态和界面状态

UI 不得用临时视觉变量代替会影响业务的数据状态。至少需要保持以下状态关系：

```text
setupRequired
selectedPlace
selectedBackpack
packedItems
outingStatus
arrivalReviewState
itemStatus
pendingDeadline
```

`arrivalReviewState` 必须在应用恢复或重新进入页面后仍然可推导，不能只依赖一个视图的临时 `@State`。动画状态不能决定库存归属。

定位和权限必须有明确降级：

- 始终定位：后台围栏提醒。
- 只有前台定位：说明限制，保留手动确认。
- 定位关闭：显示系统设置入口和手动地点选择。
- 通知关闭：页面内仍显示到达状态。

## 9. 动效系统

### 9.1 动效职责

每个动效必须说明操作、状态变化或下一处视觉焦点。不能为了“看起来高级”加入无语义的持续动画。

### 9.2 时间和曲线

| 场景 | 时长 | 曲线 |
| --- | ---: | --- |
| 按压反馈 | 120–160ms | easeOut |
| 行状态变化 | 160–220ms | easeInOut |
| 数字变化 | 180–240ms | easeOut |
| 分类切换 | 220–280ms | easeInOut |
| 背包对象切换 | 280–380ms | spring / easeOut |
| 地点详情 sheet | 系统默认 | 原生 sheet |
| 完成状态 | 260–360ms | easeOut |

### 9.3 关键动效

背包切换时当前对象向中心稳定移动，前后对象同步缩放和改变透明度；不旋转成完整 3D 场景，不自动播放。

物品加入背包时，先立即更新行状态，再让缩略图短暂缩放到 1.04，数量做数字过渡；可选的小型图标位移不超过 280ms。快速连续添加时，列表不能跳动。

页面切换保留品牌抬头和地点语境，只使用轻微淡入和 8–16pt 位移，不使用整页大幅甩动。

进入地点范围时，地图标记只扩散一次，随后显示“已到达”；不自动打开检阅清单，不自动移动库存。

完成清点时使用一次轻量勾选描边或状态图标强调。不使用粒子、烟花、持续循环或阻塞式庆祝动画。

### 9.4 减少动态效果

使用 `accessibilityReduceMotion`：位移、缩放和模糊替换为淡入淡出；`matchedGeometryEffect` 替换为即时出现和状态更新。数据保存、导航和操作顺序不受影响。

## 10. 3D 图标、照片和主题

### 10.1 3D 图标

首期使用静态 2.5D/伪 3D 图标：统一透视角度、左上光源、底部阴影、画布和安全边距。优先服务背包、房屋、相机、镜头和电脑等对象识别。

列表中的 3D 图标最大尺寸为 52pt；当前聚焦对象可以使用 120–190pt。普通列表仍可使用 SF Symbols。

### 10.2 用户照片

照片和 3D 图标使用相同的容器尺寸、裁剪比例和圆角。照片是直接识别来源，不强制转换为 3D，也不在没有云端能力时伪装成 AI 抠图结果。

### 10.3 主题系统

主题改变对象场景和插画素材，不改变状态语义、布局或交互顺序。首批主题预留：

- Gowith Base：薄荷绿、暖白、黑色。
- Photographer：紫色/青色，摄影设备图标。
- Student：蓝色/橙色，电脑、手机、书本图标。
- Everyday：珊瑚/黄色，钥匙、雨伞、钱包图标。

主题不能隐藏状态文字、改变导航顺序或增加复杂装饰导致清点变慢。

## 11. 无障碍和响应式

- 主要按钮和行级操作的点击区域至少 44x44pt。
- 图标按钮必须有中文 `accessibilityLabel`。
- 状态通过 `accessibilityValue` 读取完整语义，如“钥匙，待确认，9 月 27 日前”。
- Dynamic Type 放大后，重点场景允许增高，列表行允许两行文本。
- 横向滚动只允许出现在背包对象轨道和分类轨道。
- VoiceOver 顺序应为：地点 -> 当前对象 -> 状态摘要 -> 主操作 -> 列表。
- 深色模式需要独立的高对比场景色；不能直接反转浅色 Token。

## 12. 组件和代码迁移表

| 旧概念 | 新概念 | 处理方式 |
| --- | --- | --- |
| `GowithCard` 通用容器 | `GowithFocusField` / 连续列表 | 不再把所有区块包成卡片 |
| `GowithJourneyDashboard` | `GowithContextStrip` | 只保留地点、背包和数量的最小语境 |
| 背包大卡片 | `GowithObjectRail` + `GowithFocusField` | 当前对象突出，邻近对象露出 |
| 分类胶囊 | `GowithCategoryRail` | 固定高度、图标+文字、横向滚动 |
| 普通物品行 | `GowithInventoryRow` | 连续列表，右侧单一动作 |
| `GowithStatusBadge` | `GowithStateMark` | 状态文字、图标和截止时间统一 |
| 自定义悬浮 Dock | 系统 `TabView` | 不恢复毛玻璃 Dock |
| 悬浮 GO | `GowithBottomAction` | 主操作跟随当前流程 |
| 黑色顶部主题卡片 | `GowithBrandHeader` | 删除黑色容器，保留品牌抬头 |
| 临时到达页状态 | 持久化或可推导的行程状态 | 不让临时动画状态决定业务数据 |

迁移期间旧组件可以继续编译，但新页面不得继续扩展旧语义。新页面优先创建新组件，迁移完成后再删除旧包装组件。

## 13. 实施计划

### Phase 0：冻结规则

- 将本文件设为唯一设计源。
- 集中新增颜色、间距、动画和场景 Token。
- 不修改数据模型和定位业务。
- 建立 iPhone 17、窄屏和 Dynamic Type 验收基线。

### Phase 1：重建应用外壳

- 重做品牌抬头和页面标题。
- 统一四项图标导航。
- 重做主操作底部区域。
- 删除旧的通用大卡片层级。

### Phase 2：重做背包页

- 实现 `GowithObjectRail`。
- 实现彩色聚焦场景。
- 当前背包和左右邻近背包形成稳定空间层级。
- 把物品列表放回连续内容流。

### Phase 3：重做物品库

- 分类轨道。
- 当前背包语境条。
- 重点物品场景。
- 连续物品列表。
- 装入/移除的即时状态反馈和可中断动效。

### Phase 4：重做地图和出行

- 地图最小化视觉。
- 地点点击底部面板。
- 出行中地图优先。
- 到达提醒与检阅底部动作分离。
- 回家清点和待确认流程保持数据完整。

### Phase 5：重做我的和主题基础

- 地点库存看板。
- 统计与偏好设置。
- Base、Photographer、Student、Everyday 四类主题 Token 预留。

### Phase 6：验收和清理

- 测试快速连续添加物品。
- 测试切换地点和背包后的状态连续性。
- 测试出行中后台恢复和到达后重新打开。
- 测试定位权限、通知权限和手动确认降级。
- 测试减少动态效果、VoiceOver、Dynamic Type 和深色模式。
- 删除已经不再使用的旧 UI 包装组件。

## 14. 本轮边界

以下能力暂不阻塞本轮 UI 重构：复杂实时路线轨迹、地铁站和特殊景点的完整场景动画、动态 3D 人物、云端 AI 抠图和 2.5D 渲染、主题商店、账号体系、多设备同步和社交分享。

## 15. 最终验收标准

- 用户打开页面能看到当前地点、当前对象和下一步动作。
- 每个页面只有一个视觉主焦点和一个主要操作。
- 背包使用横向对象轨道，当前对象最大，邻近对象露出一部分。
- 物品库具有分类横向轨道和连续列表。
- 彩色场景用于对象和优先级，不成为信息噪音。
- 地图只突出地点、当前位置和范围，点击地点后出现统一库存面板。
- 到达后先提醒，用户主动开始检阅。
- 状态同时由图标、文字、数量或日期表达。
- 页面切换保持地点、背包和物品的空间连续性。
- 动效可中断，减少动态效果后仍然完整可用。
- 不存在旧的黑色顶部主题卡片、毛玻璃底部 Dock 或悬浮 GO。
- 动画、3D 图标和彩色场景不影响清点效率、定位反馈和数据正确性。
